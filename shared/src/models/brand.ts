export interface TeamBrand {
  logo: string;          // Emoji selection (e.g. "🦁")
  primaryColor: string;  // Hex color code (e.g. "#ef4444")
  secondaryColor: string;// Hex color code (e.g. "#b91c1c")
  banner: string;        // CSS gradient string
}
